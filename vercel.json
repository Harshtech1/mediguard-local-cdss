{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "buildCommand": "cd mediguard && npm install && npm run build",
  "outputDirectory": "mediguard/dist",
  "framework": "vite"
}
